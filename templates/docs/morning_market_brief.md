# Morning Market Brief: <as-of date>

- **As-of date:** YYYY-MM-DD
- **Watchlist:** topics and tickers this run covered
- **Providers:** which of `newsapi` / `alpha_vantage_news` / `finnhub_news`
  were enabled for this run

> Generated by `render_morning_brief` (spec `0059-morning-market-brief`) from
> real pulled headlines plus agent-written analysis. The headlines and
> sentiment-rollup sections below are computed; the Views & Analysis section
> is written by `agents/economists/morning_brief_writer/` and is not
> auto-promoted anywhere — see Review Status.

## Headlines

Grouped by watchlist topic/ticker, most recent first, deduplicated by URL
across providers. Each item names the provider(s) it came from.

### <topic or ticker>

- `<headline>` (`<url>`) — *<source name>*, <published_at UTC>, via
  `<provider>`

## Sentiment Rollup

Mean `sentiment_score` per ticker, from Alpha Vantage coverage only — absent
for a topic/ticker Alpha Vantage didn't cover, never shown as zero.

| Ticker | Mean sentiment | Label | Articles |
| --- | --- | --- | --- |
|  |  |  |  |

## Views & Analysis

*(Written by `agents/economists/morning_brief_writer/`, grounded in the
Headlines and Sentiment Rollup sections above plus the day's upstream
`agents/economists/*` reads. Every claim here should trace to a headline
above or a named upstream report — `instructions/knowledge_base.md`'s
grounding standard applies to this section same as any other.)*

## Sources & Citations

Every headline used above, listed once more as a flat citation list — the
form a staged research candidate's `citation` field is built from.

## Review Status

- **Staged as:** `research_local/inbox/morning_brief/<source_run>.yaml`
  (`review_status: pending_review`)
- This file is **not** committed to the repository and is **not**
  auto-promoted into any durable store — per
  `specs/0056-market-research-knowledge-base/spec.md`'s Non-Goals, real
  generated research content stays local until a human reviews it.
