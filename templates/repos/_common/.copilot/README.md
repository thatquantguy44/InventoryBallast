# Copilot / assistant configuration

Editor-assistant guidance for this repository. Kept separate from `.agents/`
because these are *ambient* instructions applied to every completion, not
addressable roles you invoke.

- `instructions.md` — repo conventions every suggestion should follow.
- Keep it short. Long files get truncated, and the rules that matter most are
  the ones that survive truncation.
