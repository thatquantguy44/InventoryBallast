# Roadmap

The document a new owner reads first. The `handoff-sync` gate requires every
spec to be referenced here, and blocks a commit that adds a spec without an
entry — so this file cannot silently fall behind the work.

It checks that a spec is *mentioned*, not that what you wrote about it is true.
Silence is gated; staleness is still on you.

## Snapshot

<!-- What exists today, in three sentences. Counts belong here; the
     doc-counts gate will flag them when they drift. -->

## What's next (prioritized)

1. **<Initiative>** — one line on why it matters.
   - **<Phase / spec `NNNN`>** — status and what remains.

## Open questions for the owner

- <Decisions that need a human, with the tradeoff stated.>

## Risks

- <What could make the above wrong.>
